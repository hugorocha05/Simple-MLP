# Simple-MLP
A simple Multi-Layer Perceptron (MLP) implemented entirely from scratch using only vanilla Python. The project focuses on understanding the fundamental mechanics of neural networks, including neurons, layers, activation functions, weights, biases, parameter initialization, and forward propagation.
